{
  "name": "animation-block-wrapper",
  "version": "1.1.0",
  "description": "GPL-2.0-or-later Gutenberg wrapper block with lightweight animations.",
  "license": "GPL-2.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'Add lint tooling as needed'",
    "test": "node --test tests/**/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
